<!DOCTYPE html>
<html>
  <head>
    <title>TaskBeam</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>
  <body>
    <%= flash.each do |name, msg| %>
      <%= content_tag(:div, msg, class: "flash #{name}") %>
    <% end %>

    <%= yield %>
  </body>
</html>
