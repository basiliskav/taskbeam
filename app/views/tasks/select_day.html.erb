<h1>Select Day for Tasks</h1>

<%= form_with url: show_by_day_tasks_path, method: :post, local: true do |form| %>
  <div class="field">
    <%= form.label :day %>
    <%= form.date_field :day, required: true %>
  </div>
  <div class="actions">
    <%= form.submit "Show Tasks" %>
  </div>
<% end %>